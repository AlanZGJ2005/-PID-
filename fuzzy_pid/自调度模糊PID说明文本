模糊自调度 PID ：
示例场景：2026 年电赛 H 题“车载平衡小球”。管道左端为 -10，右端为 +10，平衡点为 0。K230 视觉检测小球位置，步进电机摆动管道提供回调力。

1. PID 的本质
PID 的本质需求是：系统偏离目标后，生成一股“回调力”，把系统拉回目标。
工程上可以理解为：
总回调力 = P 的力 + I 的力 + D 的力
P：主力军
误差有多大，出力就有多大。误差大就猛推，误差小就轻推，误差为零就不出力。
I：记账员
把历史上每一笔小误差攒起来，专门消灭 P 搞不掉的顽固误差。只要误差还没归零，积分量就持续增加，直到误差清零。
D：观察员，也是刹车片
不管误差多大，只看误差变化有多快。误差正在快速减小时，说明系统正在猛冲回来，D 会提前施加反向力，防止冲过头和震荡。
推车类比
- P 看到车偏了 5 米，出 5 份力；偏 1 米，就出 1 份力。
- I 发现推了半天还差 0.1 米，把每次“差一点点”攒起来，额外加力。
- D 不看距离只看速度，发现车猛冲回来要冲过头，赶紧反向推一把刹车。
三股力同时作用，车又快又稳地回到目标。
2. 静态 PID 与临界比例度法
校园比赛中，很多时候允许系统进入等幅振荡，因此常用临界比例度法。
核心思路是：
故意把系统“逼疯”：关掉 I 和 D，只保留 P，不断加大 P，直到系统出现持续等幅振荡。记录此时的 P 和振荡周期，再套公式反推 PID 参数。
放在 H 题小车上
1. 先把 I 关掉，D 设为 0，只保留 P。
2. 从小到大慢慢加大 P，让小球出现左右持续滚动，并且管道倾斜的回调力能够控制小球加速度。
3. 用二分法逼近临界 P。
不同现象对应的判断：
- 小球慢吞吞滚回去，差一点停不住：P 太小，继续加。
- 小球冲过头幅度越来越大，晃好几秒才停：P 偏大，往回退。
- 小球较快滚回中心，轻微超调后衰减停住：快到了，继续加。
- 小球匀速来回滚动，幅度基本不变，不衰减也不发散：到达临界点。
4. 记录小球从左到右再回到左的完整周期，量 5 到 10 个周期取平均。
5. 套公式得到初始 PID 参数。
静态 PID 的两个常见坑
第一个坑：算出来的参数还是高频微抖。原因是临界比例度法得到的参数偏激进，钢球轻质量、低摩擦，容易抖。解决办法是初始值先打 6 到 8 折，如果还抖，优先增加 D。
第二个坑：小球能停住，但总差 0.5 厘米回不到中心。这是稳态误差，应该让 I 上场。逐步减小积分时间，加强 I 的作用，同时必须对 I 做限幅或抗饱和。
3. 为什么需要动态 PID
静态 PID 只有一套固定参数。但小车平衡小球时，小球在不同位置、不同速度下，需要的拉力和刹车并不一样：
- 小球偏离很远：需要更大的回调力。
- 小球已经快速冲回来：继续猛拉会冲过头，应该收一点 P，加一点 D。
- 小球在中心附近：需要更精细的定位，不能大动作。
所以更好的做法是：Kp 和 Kd 不固定，而是根据误差 E 和误差变化率 EC 实时调整。这就是增益自调度 PID。
三种形态的进化路线
第一种是硬阈值 P 调度。它的问题是输出会跳变、只调 P，而且不容易区分单调发散和振荡。
第二种是模糊 P/D 调度。它连续平滑，用 E 和 EC 两个维度判断，P 和 D 协同调整。本文实现的就是这一层。
第三种是自整定。它使用目标函数和搜索算法，脱离人工规则。
4. 模糊自调度 PID 的工作流程
4.1 把误差分成 7 个模糊档
连续误差不好直接写规则，所以先分成 7 档：
负大、负中、负小、零、正小、正中、正大。
对应模糊论域为 -3 到 +3。
“模糊”的意思是：一个值可以同时属于两档。例如 1.5 不完全是正小，也不完全是正中，而是“半个正小加半个正中”。
4.2 用 KE 和 KEC 把真实物理量缩放进论域
真实误差先乘以 KE，误差变化率先乘以 KEC，得到模糊论域里的值。
对于 H 题，误差满量程约为 10，误差变化率满量程约为 5，因此：
KE 等于 3 除以 10，结果是 0.3。
KEC 等于 3 除以 5，结果是 0.6。
例如小球偏左，位置为 -5，误差为正 5。乘以 KE 后得到 1.5，落在正小和正中之间。
4.3 三角隶属度：这个值“像”哪一档
隶属度表示当前值属于某个模糊档的程度。
计算方法是：用 1 减去“当前值与档中心距离的绝对值”，如果结果小于 0 就取 0。
例如模糊值为 1.5：
- 对正小，中心为 1，隶属度为 0.5。
- 对正中，中心为 2，隶属度为 0.5。
- 对其他档，距离都大于等于 1，隶属度为 0。
所以 1.5 的权重是：正小占一半，正中占一半。
4.4 规则表：不是公式算出来的，是经验写出来的
规则表是两张 7 乘 7 的表。行表示误差 E，列表示误差变化率 EC，数值范围是 -3 到 +3。正数表示增加增益，负数表示减少增益。
KP 规则表的设计直觉是：球越远越加拉力，但球已经在快速往回冲时，要收一点力。
KD 规则表的设计直觉是：速度越快，刹车越猛；中心附近额外加一点阻尼，防止冲过头。
规则表不是由某个公式硬算出来的，而是把控制经验翻译成整数。你可以根据实际车况修改它。
4.5 加权平均解模糊
对每个有贡献的格子，计算权重：
权重等于误差隶属度乘以误差变化率隶属度。
然后计算：
KP 增量等于每个格子的权重乘以该格子 KP 规则值，再求和，最后除以总权重。
KD 增量同理。
每个误差值最多激活相邻两个模糊档，每个误差变化率也最多激活两个模糊档，所以实际最多只有 2 乘 2 等于 4 个格子有贡献。
4.6 合成最终增益
最终增益采用“基准值加模糊增量”的方式：
Kp 等于基准 Kp 加上输出因子乘以 KP 增量，再做上下限幅。
Kd 等于基准 Kd 加上输出因子乘以 KD 增量，再做上下限幅。
限幅是安全约束，不能省略。
4.7 I 项：固定增益加反算抗饱和
本方案不模糊调度 Ki，而是让 Ki 固定。
抗积分饱和采用两层：
第一层是反算抗饱和。输出被限幅后，把被砍掉的部分按比例拉回积分器，让积分器平滑退出饱和。
第二层是宽安全兜底限幅。只防止数值发散或溢出，不参与正常抗饱和。
执行顺序是：先累加积分，再做反算抗饱和，最后做宽限幅。
5. 用 H 题数据完整走一遍
5.1 坐标约定必须定死
误差等于目标位置减去小球当前位置，也就是 0 减去小球位置。
误差变化率等于误差的导数，也就是负的小球速度。
因此：
- 球在左，误差为正。
- 球在右，误差为负。
- 球向右滚，误差变化率为负。
- 球向左滚，误差变化率为正。
5.2 K230 数据怎么转成误差
假设 K230 传来的数据格式是：第一位表示方向，后三位表示偏移量。
- 0 表示不偏。
- 1 表示左偏。
- 2 表示右偏。
例如 1020 表示左偏 20。因为第一位是 1，代表左偏；后三位 020 代表偏移 20。
此时小球位置为 -20，误差为正 20。
实际工程中，K230 的原始量纲要按标定换算成厘米。本说明统一按管道 -10 到 +10 理解。
5.3 例子：球在 -8 厘米，正以每秒 6 厘米向右回中
误差为正 8，误差变化率为负 6。
误差乘以 KE 后为 2.4，落在正中和正大之间。
误差变化率乘以 KEC 后为 -3.6，限幅后为 -3，完全落在负大。
此时应重点看正大行、负大列。
KP 规则值为 1，表示收一点 P，别猛推。
KD 规则值为 3，表示 D 拉满，准备刹车。
加权平均后，KP 增量约为 0.4，KD 增量为 3.0。
这正好实现了“小球已经快速回中时，收 P、加 D”的控制目标。
5.4 一个非常容易犯的错：表坐标看反
很多人会误以为“球偏左很远”对应负大。但按前面的约定，球偏左时误差为正，所以应该看正大行。
负大行、负中列实际表示的是：球在右边很远，并且继续向右滚。这时加大 P 才是正确的。
所以调试前，必须先把误差、误差变化率的符号和规则表方向核对清楚。
6. 参数怎么调
6.1 三个灵敏度旋钮
- KE：管“多偏算偏”。KE 大，球偏一点点就被当成大误差，反应猛;KE 小则迟钝。
- KEC：管“多快算快”。KEC 大，一点点速度就算冲得快，D 更容易介入。
- KP 输出因子和 KD 输出因子：管“模糊增量一次最多调多大劲”。
6.2 基准值
- KP_BASE：静止或中心附近的基础 P。
- KD_BASE：基础阻尼。
- KI_FIXED：固定积分增益。
6.3 限幅
需要设置 KP 上下限、KD 上下限、输出上下限和积分安全限幅。限幅是安全约束，不能省略。
6.4 规则表怎么设计
规则表是把控制经验写下来：
- 球越远，P 越大。
- 球正在远离中心，P 继续加。
- 球正在快速回中，P 收一点。
- 速度越快，D 越大。
- 中心附近，D 略加，防止冲过头。
如果实车发现“大偏差加快速回中”仍然冲过头，可以把对应位置的 KP 再降一点，或者把对应位置的 KD 再升一点，也可以适当增大 KD 输出因子或 KD 上限。
6.5 抗饱和参数
反算抗饱和系数越大，退出饱和越快。
积分安全限幅建议取输出上限的两倍左右。
7. 工程上必须做好的保护
第一，误差变化率要做低通滤波。K230 视觉有像素噪声，直接差分会让 D 项剧烈抖动。
第二，dt 要保护。第一次调用或调度异常时，dt 可能为 0，必须加下限保护。
第三，首次调用要特殊处理。首次调用让上一次误差等于当前误差，误差变化率从 0 开始，避免假大变化率冲击 D 项。
第四，输入误差要保护。传感器异常或误差超范围时，保持上一次输出，不要乱动。
第五，积分宽限幅。正常抗饱和交给反算，宽限幅只做安全兜底。
8. 什么时候值得用模糊 PID
如果你脑子里已经有“这种偏差加一点、那种速度刹一点”的经验，模糊规则表正好可以把这些经验写下来，值得使用。
如果系统非线性强、工况多，比如车载场景下加速、刹车、转弯、上下坡，模糊控制能覆盖多种组合。
但如果简单连续函数已经够稳，就不必上模糊。否则 49 格规则表加上多个量化因子，调参工作量会陡增。比赛时间紧时，函数式往往性价比更高。
9. 总结
模糊自调度 PID 本质上没有脱离 PID，只是让 Kp 和 Kd 会“看情况”调整。
误差 E 决定“该出多大力”。
误差变化率 EC 决定“该不该刹车”。
它比固定 PID 更聪明，比硬阈值调度更平滑。
放在 H 题小车上，就是一句话：
小球偏得远就多拉一点，冲回来太快就提前刹车，中心附近就轻手轻脚。
真正调车时，先把坐标约定和规则表方向核对清楚，再用 KE、KEC、基准增益、输出因子和限幅这些“旋钮”逐步收敛。代码只是骨架，最终手感还是来自对小车物理特性的观察。
